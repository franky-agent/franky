# Secret Management

The needed secrets for providers or tools should be fetched on startup and only be avilable to franky in memory there should be not stored on disk or as environment variable. We need a zero trust approach for this because the bash tool of franky itself can read the environment variables and files, so we need to make sure that the secrets are not be accessible for the bash tool and the model.
