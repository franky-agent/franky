# Franky process restarts itself — remaining work (v2/v3)

**Status:** pending — v2 scope deferred, v3 scope deferred

**Affects:** `src/coding/modes/interactive.zig`, `src/agent/guardrails/finish_task.zig`, `src/bin/franky_profile.zig` (watchdog), `build.zig` (watchdog step)

---

## Remaining scope

### Interactive mode support (v2)

The terminal loop in `src/coding/modes/interactive.zig` can check the
`restart_requested` flag between turns. When the flag is set:

1. Persist session state (transcript, settings).
2. Close any listeners / IO handles.
3. Call `restart.spawnAndExit(io)` — same mechanism as proxy mode.

The interactive mode already has a main loop structure; the change is adding a
flag poll before each turn prompt.

### `finish_task` guardrail extension — `restart: bool = false` (v2)

In `src/agent/guardrails/finish_task.zig`, add a `restart: bool = false`
parameter to the `finish_task` tool call. When `true`, after the termination
check passes and the final message is emitted, the guardrail sets the session's
`restart_requested` flag and (optionally) signals the outer shell script or
watchdog to re-spawn.

```zig
// Pseudocode sketch
pub const FinishTaskParams = struct {
    commit_message: []const u8,
    summary: []const u8,
    restart: bool = false,  // ← new field
};
```

When `restart = true`, the guardrail calls a shutdown path that triggers
`restart.spawnAndExit(io)` after the reply is sent.

### Integration test — PID changes after restart (v2)

A multi-step test that:

1. Starts franky in proxy mode (unique port).
2. Captures the PID of the running process.
3. Issues `POST /restart`.
4. Waits for the old process to exit (polls `/health` until it fails).
5. Polls `/health` on the same port until the new process responds.
6. Verifies the new PID differs from the old PID.

This requires the test harness to manage a sub-process lifecycle. A port range
or ephemeral port avoids collisions.

### Watchdog / `franky --as-watchdog` (v3)

Approach D from the original design — a tiny bootstrap that spawns franky,
waits for it to exit, and re-spawns it when exit code signals "restart".

```
watchdog ──► spawn franky args...
              │
              ├── franky finishes a feature, recompiles itself
              │   └── calls finish_task with restart flag
              └── franky exits(42)
         watchdog sees exit(42) ──► spawn franky args... (new binary)
```

**Pros:**
- Absolutely clean restart — no signal-safety issues, no fd games
- Works across binary replacements (the watchdog re-reads `argv[0]` from PATH)
- Systemd / Docker already provide this pattern (`Restart=always`)

**Cons:**
- Need a wrapper binary or script (`franky-restartd` or `franky --as-watchdog`)
- Two processes in memory during the handoff
- Marginal complexity for the restart logic

### Port-conflict test (v3)

Start two franky instances on the same port, one restarts, verify the child can
still bind (`reuse_address` guards this).

---

## Design decisions (preserved from original)

### Trigger surface

- **Implemented:** `/restart` slash command + `POST /restart` HTTP route.
- **Deferred (v2):** `finish_task` `restart` bool field.
- **Deferred (v2):** Interactive mode flag poll.
- **Deferred (v3):** Watchdog / `--as-watchdog` mode.

### Approach: spawn-and-exit (Approach B)

All remaining items build on the same mechanism: close listen socket, spawn
child, brief head start, exit. See the archived implementation doc for details.

---

## Next steps

1. Add `restart` flag poll to interactive mode loop.
2. Add `restart: bool = false` to `finish_task` guardrail.
3. Write integration test that verifies PID changes after restart.
4. *(v3)* Implement watchdog binary.
5. *(v3)* Port-conflict stress test.
